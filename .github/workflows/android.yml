name: Android ARM64 Pikafish (The Final Boss)
on:
  push:
    branches: [ fix/android-tls-alignment ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Manual NDK r26
        run: |
          cd /tmp
          wget -q https://dl.google.com/android/repository/android-ndk-r26-linux.zip
          unzip -q android-ndk-r26-linux.zip
          mv android-ndk-r26 $HOME/android-ndk
          echo "ANDROID_NDK_ROOT=$HOME/android-ndk" >> $GITHUB_ENV

      - name: Build Pikafish (Force Clang)
        run: |
          cd src
          export TOOLCHAIN=$ANDROID_NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64
          
          # 关键：直接在 make 命令行中强制指定编译器路径和 TLS 参数
          make -j$(nproc) ARCH=armv8 build \
            CC=$TOOLCHAIN/bin/aarch64-linux-android21-clang \
            CXX=$TOOLCHAIN/bin/aarch64-linux-android21-clang++ \
            EXTRA_LDFLAGS="-Wl,-z,max-page-size=65536"

      - uses: actions/upload-artifact@v4
        with:
          name: pikafish-android-arm64
          path: src/pikafish
